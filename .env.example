# Contact form delivery (app/api/contact/route.ts).
# Without RESEND_API_KEY the form returns an error and shows the
# "escríbenos a info@nusku.cloud" fallback, so leads are never silently dropped.
RESEND_API_KEY=
CONTACT_TO_EMAIL=info@nusku.cloud
CONTACT_FROM_EMAIL=web@nusku.cloud
